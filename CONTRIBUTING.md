# 贡献指南

欢迎提交 Issue 和 Pull Request！

## 开发环境

```bash
git clone https://github.com/deepvoce/fnmedia-monitor.git
cd fnmedia-monitor
pip install -r requirements.txt
```

## 运行

```bash
export FNMEDIA_DB_PATH=/path/to/trimmedia.db
python main.py
```

## 提交 PR

1. Fork 本仓库
2. 创建功能分支 (`git checkout -b feature/your-feature`)
3. 提交更改 (`git commit -m 'Add your feature'`)
4. 推送到分支 (`git push origin feature/your-feature`)
5. 创建 Pull Request

## 提交规范

- `feat:` 新功能
- `fix:` 修复问题
- `docs:` 文档更新
- `perf:` 性能优化
- `refactor:` 代码重构
