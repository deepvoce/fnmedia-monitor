# 更新日志

## v2.0 (2026-07-11)

### 🎨 前端全面重设计 · Bento Dashboard

> V2.0 聚焦视觉焕新与动画体验，采用 Bento 网格 + 渐变光晕风格，并在首轮 Bento 基础上完成深度视觉精修

- **Bento 网格布局** — 12 列模块化卡片错落排列，卡片尺寸有节奏感，视觉层次丰富
- **渐变光晕背景** — 紫/青/绿三色 aurora 流动光晕缓慢漂移，营造氛围感
- **活力渐变色彩** — 紫蓝 (#6366f1) / 紫罗兰 (#8b5cf6) / 青蓝 (#06b6d4) / 翠绿 (#10b981) 多彩渐变系统，深浅主题协调
- **品牌标识升级** — 原始 LOGO 透明化处理 + 深浅双版本（logo.png / logo-light.png）随主题自动切换

### ✨ 新增功能模块

- **类型分布环形图** — 直观展示媒体类型占比，中心显示总数（数据来自 `/api/stats` 的 `type_distribution`）
- **用户排行榜** — 前 3 名金/银/铜渐变徽章，行背景条形按播放次数比例展示
- **访客日志表** — IP / 位置 / 设备（浏览器·OS）/ 路径 / 时间五列展示，未启用日志时优雅空状态
- **IP 地理位置地图** — Leaflet 轻量地图，圆点大小按访问次数缩放，深浅主题自动切换瓦片层（CartoDB Positron / Dark Matter）

### 🎬 动画效果

- **错落入场** — 卡片按顺序淡入 + 上移 + 缩放，60ms 错落间隔
- **数字计数动画** — 统计卡数值 800ms 缓动递增
- **骨架屏** — 当前观看加载中显示 shimmer 占位
- **悬停微交互** — 卡片上浮 + 发光边框 + 投影加深
- **进度条动画** — 900ms 缓动填充
- **图表入场** — Chart.js 旋转 + 缩放入场动画
- **滚动揭示** — 下半部分卡片随滚动渐入（IntersectionObserver）
- **实时脉冲** — "LIVE" 指示器脉冲心跳
- **刷新旋转** — 刷新按钮图标旋转
- **主题平滑过渡** — 颜色 400ms 渐变切换

### ♿ 可访问性

- 支持 `prefers-reduced-motion`，减少动画偏好下禁用光晕漂移与入场动画

### 🔍 视觉精修（Bento 首轮打磨）

- **Carousel 阻尼翻页** — 收藏记录 / 下载任务 / 系统状态三处列表固定 4 条/页，滚轮 + 箭头 + 圆点三种翻页方式，350ms 滚轮锁定 + 500ms cubic-bezier 阻尼过渡，底部页码提示固定不随末页漂移
- **浅色主题统一** — body 改为纯淡蓝白渐变 (#eef1fb → #e8ecf8 → #e3e8f5 → #edeef9)，sidebar / topbar 统一 rgba(248,250,255,0.96) 衔接，brand 透明继承，消除三区域色块割裂
- **LOGO 透明化 + 双版本** — PIL 移除深蓝背景 (#182c69) 并裁剪多余边距（381×191 → 295×120），生成 logo-light.png 将白色文字转深色 #14142e，深浅主题自动切换 LOGO 图源
- **周活动热力图排版** — 优化 cell 高度 / 间距 / 颜色层次，避免密集压抑
- **用户排行皇冠** — 第一名头像上方叠加金色皇冠 SVG，呼吸光晕动画
- **侧边栏导航增强** — nav-item 可点击锚点跳转 + scroll spy 高亮 + scroll-margin-top: 84px 防止定位遮挡
- **Hero 卡片玻璃拟态** — 改为 backdrop-filter: blur(20px) 玻璃风格，降低光晕透明度，与整体风格统一
- **hover 效果去浮夸** — 多处悬停效果收敛为 scale(1.02) + 轻微发光，避免视觉夸张

### 🛠 技术改进

- 引入 Leaflet 1.9.4（轻量地图库，~150KB）
- Chart.js 4.4.0 渐变色填充与自定义插件（环形图中心文字）
- 单文件架构保留（符合项目轻量约定），文件增至 ~1000 行仍可控

---

## v1.1 (2026-04-01)

### ⚡ 性能优化

- **API 响应缓存** — `/api/stats`、`/api/logs`、`/api/locations` 加入 30 秒内存缓存，页面刷新从「等几秒」变「秒开」
- **批量层级查询** — `get_item_hierarchy()` 从逐条递归改为批量一次查完，数据库查询次数减少 ~90%
- **IP 查询去重** — `/api/locations` 内部 IP 查询去重，同一 IP 只查一次，响应速度提升 50-98%
- **IP 超时缩短** — 从 3s 降至 2s，超时更快返回

### ✨ 新增功能

- 刷新按钮支持强制刷新（绕过缓存获取最新数据）
- 首次加载自动获取最新数据
- 环境变量 `CACHE_TTL` 可配置缓存时间（默认 30 秒）

### 🔧 修复

- IP 查询超时从 3s 缩短至 2s，减少卡顿

### 📦 部署

- 新增 `Dockerfile`，支持直接 `docker build` 构建镜像
- `docker-compose.yml` 保留为参考，推荐使用构建镜像部署

---

## v1.0 (2026-03-17)

### ✨ 初始版本

- 实时监控当前播放
- 播放历史记录
- 统计图表（按类型、年份、质量分布）
- 存储分析
- 访客日志与地理位置可视化
- 收藏与下载管理
- 深色/浅色主题切换
- Docker 部署支持
